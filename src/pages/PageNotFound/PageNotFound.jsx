import React from 'react';
import PropTypes from 'prop-types';

const PageNotFound = () => {
    return (
        <div>
            Page not found
        </div>
    );
};

export default PageNotFound;